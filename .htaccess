<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward all requests to /public so URL does not require "/public".
    RewriteCond %{REQUEST_URI} !/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
